--- ui/src/tests/GraphInspection.test.tsx
+++ ui/src/tests/GraphInspection.test.tsx
@@ -120,7 +120,7 @@
       screen.getByRole("button", { name: "Inspect" }).click();
     });

-    await waitFor(() => expect(screen.getByRole("button", { name: "Select git" })).toBeInTheDocument());
+    await waitFor(() => expect(screen.getByRole("button", { name: "Select demo" })).toBeInTheDocument());
     expect(screen.getByRole("button", { name: "Select AGENTS.md" })).toBeInTheDocument();

     await act(async () => {
